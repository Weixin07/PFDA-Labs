4+5

a=10
b=20
c=a+b
d=a+c

installed.packages() #To view all of the packages that's already installed
install.packages("crayon") #To install the package named 'crayon'
library(crayon) #To load the package 'crayon'

install.packages("plotrix") #same, its just for practice
library(plotrix)

detach("package.crayon") #To unload the package 'crayon'
remove.packages("crayon") #To uninstall the package named 'crayon'
